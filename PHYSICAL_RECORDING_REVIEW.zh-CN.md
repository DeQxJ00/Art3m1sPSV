# 2026-09-07 实机录像核查

录像：`Z:/Record/OBS/2026-09-07 22-29-21.mp4`，232.267 秒，1280×720、60 FPS 录制。用户确认使用**存档图片修复前**的 VPK；因此本录像中的低帧不能归因于后来引入的每帧截图等待。下面 FPS 是录像叠加层读数，不是逐帧 GPU 耗时测量；录制 60 FPS 也不表示游戏始终渲染 60 FPS。

## 观察

- 约 00:16–00:24：启动黑屏，叠加层出现 0 FPS，缺少加载反馈。
- 约 01:44–02:00：房间、镜头切换出现明显波动，抽样见 7 FPS；静止正文又可接近 60 FPS。
- 约 03:14–03:20、03:23–03:28、03:35–03:40：黄色箭头逐步覆盖/退出画面，叠加层多次为约 2–5 FPS；动画期间有长时间停留在相邻帧的现象。
- 章节跳过确认在开场及后续章节都出现。仅凭无手动槽位，不能证明全局数据或已读数据为空。

复核图在 `build/recording-review/overview.jpg`、`overview-end.jpg`、`arrow-seconds.jpg`。最后一张每格一秒，从录像 190 秒开始，逐行阅读。

## 1. 加载进度条：已实现，待运行验收

`host-gxm/src/game_surface.cpp` 原先在视图构造函数里完成 PFS 读取和 core 初始化，无法先画出加载页面；`host_loading_show` 还是空实现。

现在构造只创建加载视图。PFS 索引读取在 pthread 工作线程执行，进度页面显示已完成资源包数/总数；通过原子的结果字段通知主线程，再显示“正在初始化引擎与启动脚本”，随后在主线程创建 runtime、加载项目。GXM/NanoVG 不进入工作线程。销毁时 join 读取线程，避免档案读取和关闭发生竞争。

进度条表示扫描、档案读取、引擎初始化等阶段，不假装精确知道剩余秒数。core 启动脚本仍是同步阶段，这一阶段长耗时还需要后续拆分；不能把进度条加入等同于加载耗时已优化。编译通过，尚未在模拟器或实机验证此新线程路径。

## 2. 原生 eboot 的缓存证据

在工作区 IDA 数据库副本上重新执行 `scripts/ida_cache_review.py`，结果在 `build/recording-review/native-cache/`。没有修改安装目录中的 eboot 或游戏资源。

| 证据 | 能确认的行为 | 不能据此推出的结论 |
|---|---|---|
| `0x81031D64` | 显示索引 `(index+1)%3`，三重显示缓冲；该路径也有 sceGxmFinish | 三缓冲就是三层场景缓存；原生完全不等待 GPU |
| `0x81031CB8`、`0x81031EF4` | 可以在显示目标与离屏目标之间切换 | 所有静态图层都已自动缓存 |
| `0x810328D8` | 宽高/格式/标志相同则提前返回；变化时才销毁、重建渲染目标和纹理 | 存在通用的多级 LRU 场景缓存 |
| `0x8103DD00` | JPEG 输入、输出及工作区有容量判断，仅容量不够才扩展，按 MiB 对齐复用 | PNG/Theora 都使用相同硬件解码路径 |
| `0x81031FC8` | 区分直接内存块释放和多个分配管理对象的回收接口 | 三个管理对象就是三层缓存；用途尚需追踪 |

准确结论是**存在多种缓冲复用和离屏机制**，尚无足够证据将它们统称为“多级场景渲染缓存”。部分调用通过虚表发生，直接 xref 为空不能说明函数未使用。

游戏自身 `system/image/cache.lua` 还具有独立的预加载策略：small/middle/large、按文件计数的上限、bindSurfaceAsync、unbindSurface。该脚本只在工作区提取并只读分析。

当前移植的具体缺口：

1. `core/src/runtime/callbacks.rs` 的 bindSurfaceAsync 实际同步调用 bindSurface；读取过程发生在主线程，isLoadingSurface 恒 false。
2. 同文件 SurfaceEntry.bytes 明确仍未被纹理加载消费。缓存会占内存，但不能完整避免后面的读取/解码工作。
3. `core/src/backend/gxm/provider.rs` 对不在 retain 集合中的资源立即删除；还没有按字节预算保留近期场景资源的 LRU。
4. `host-gxm/src/gxm_bridge.cpp` 同一纹理 ID 更新时删除旧 NVG image 再创建新 image，动态视频存在分配/上传开销。
5. 转场当前是显示面 CPU 读回、再上传；应逐步改为 GPU 离屏目标快照，避免反复 CPU/GPU 往返。

建议顺序：先把读取、图片解码、纹理上传、GPU 等待分别计时；接通预取缓存消费并保持 bind/unbind 引用语义；再做异步预取/解码、主线程定额上传；最后增加有明确内存上限且保护当前/转场资源的 GPU 缓存。PSV 不能照搬桌面“保留全部资源”。

## 3. 章节跳过：条件已核对，尚不能判定缺陷

原包 `system/adv/sceneskip.lua` 的条件是 `conf.sceneskip == 1`、当前脚本对应的 `gscr.aread[file].all` 为真、且不处于 exskip。`system/adv/fileio.lua` 将 gscr 作为全局数据独立保存/载入。这不是“必须存在某个手动存档”的规则。core 另有独立 aread.dat 行级已读记录，不能与脚本的章节 all 标志混为一谈。

待验收：为测试创建独立且全新的保存根目录，保留用户旧存档；首次 Start 不应因为旧全局记录出现已读章节跳过；完成对应章节后退出重开，跳过应可用。若干净状态仍出现确认框，再检查全局变量初始化、反序列化布尔类型、scenearead 的调用时机。不得按游戏名硬编码禁止跳过，也不直接删除用户全局数据。

## 4. OGV 箭头：双路 Theora 是独立瓶颈

从原包 root.pfs.000 选择性提取到工作区的 `アイキャッチ_01_in.ogv` 与 `_m.ogv` 均为 **Theora、YUV444P、960×540、30 FPS、0.5 秒**。前者是彩色，后者是透明遮罩；不是一个可直接交给现有 H.264 硬解器的 MP4。

`host/video.c` 当前对主视频和 mask 都指定 thread_count=1；主循环依次读包、解码两路、swscale 转换、CPU 合成 alpha，再上传 RGBA。图层视频经 core 上传，又有 RGBA 复制和纹理重建。当前每 tick 最多呈现一帧，落后时没有跳过过时帧的呈现步骤，可能把短动画拖长。没有实机分项日志前，不宣称其中某一项独占全部耗时。

建议实施：

1. 增加 mask 解码/转换、主视频解码、上传以及迟到帧分项数据，确认 0.5 秒片段实际播放耗时。
2. 彩色和 mask 成对在有界工作队列中预解码；保持两路 PTS 对齐，不在 UI 线程追赶大量 mask 帧。FFmpeg 本地 Theora 解码器声明支持 frame threading，可作为实验，但两个解码器不能无节制地争用 Vita CPU 和内存。
3. 复用双/三缓冲视频纹理，按 GPU 使用完成状态轮换；不能直接覆写仍在被 GPU 采样的内存。shader 做颜色转换/遮罩合成可减少 CPU RGBA 全屏复制。
4. 按时间戳选择最新可显示帧，跳过过时的显示/转换，不随意跳过 Theora 的依赖解码，也不靠降低播放时钟掩盖低帧。
5. 短片可预热，但一段 15 帧的 960×540 RGBA 已约 29.7 MiB；全帧缓存不是免费的。应限制缓存预算并复用进入/退出片段，不把转换后的 loose 文件写回游戏运行目录。

本轮没有修改游戏视频、没有把 OGV 替换成 MP4，也没有声称箭头低帧已解决。

## 其他优先改进与验收

- 加入实机 host.log 分项耗时与内存峰值，记录资源 IO/解码/上传/等待的最大单次耗时；用同一版本、同一时钟、同一场景重复对照。平均 FPS 不能覆盖切场景的停顿。
- 正确衔接资源绑定、解码缓存和 GPU 纹理生命周期，先解决重复工作，再扩大缓存。
- 箭头播放必须保持透明遮罩、层级、0.5 秒片段时序；不能只看全屏视频流畅。
- 补齐最小的实机回归：冷启动、第二次进入同场景、箭头进入/退出、空白全局状态的章节跳过、存档缩略图、重启读档、退出到游戏菜单。
- 文字描边、位置和转场遮罩效果仍要对照原生。录屏中人物近景的低帧还需要和普通图片切换分开记录，暂不将其归因为某一种动画引擎。

调查阶段的加载版 SHA256 为 `ED58682A637800E38D651D3B2A069F70EF70C98EF66DDA1A04D9A9918C8D4F85`，现已被后续性能版替代。上文“当前缺口”描述调查时基线；后续已接通预取字节消费、实现有限范围 Theora 后台解码和同尺寸视频纹理复用，完整修改范围、最新包哈希与测试证据见 `PERFORMANCE_CHANGES.zh-CN.md`。章节逻辑尚未修改，实机性能仍待验证。
