cmake_minimum_required(VERSION 3.16)
project(video_queue_probe LANGUAGES C)
include("$ENV{VITASDK}/share/vita.cmake" REQUIRED)
add_executable(video_queue_probe ../test.c)
target_compile_definitions(video_queue_probe PRIVATE VIDEO_QUEUE_VITA_PROBE=1)
target_compile_options(video_queue_probe PRIVATE -O2 -UNDEBUG)
target_link_libraries(video_queue_probe PRIVATE -Wl,--whole-archive pthread -Wl,--no-whole-archive c)
vita_create_self(video_queue_probe.self video_queue_probe UNSAFE)
vita_create_vpk(video_queue_probe.vpk ART3VQ001 video_queue_probe.self VERSION "01.00" NAME "Video queue probe")
