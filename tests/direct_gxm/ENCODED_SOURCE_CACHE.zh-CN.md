# 表面淘汰后保留压缩源

core 6f26d74，基于已验证注释缓存9fd98a3。实机已确认zbg27k预载过，随后反复整读约420ms、解码约310ms；原provider在成功上传后丢弃压缩源，预载take又是单次交付，GPU/CPU闲置缓存淘汰后只能读盘。

这次将已经持有的编码Vec直接移动到provider的源缓存，不复制整文件。像素预载交付同时带入原编码源；编码预载和普通读取则借用字节解码，成功上传后保留原Vec。源的透明区域证明随同保留，重新上传仍与同一文件绑定。静态图RGBA大小至少1MiB才保留源，动态上传/视频不进入；显式替换、捕获、区域更新和前缀清理会使旧源失效。

所有保留源（包括当前活动图的源）均计入既有16MiB闲置缓存估算额度，源层额外上限为该额度的1/4、最多4MiB，实际capacity和证明表都入账。GPU/解码像素维持原有共同使用顺序，源层作为最后回退保留，自身超限淘汰最旧源。没有增加总闲置额度，没有更改surface-loader队列、等待、线程、shader或GPU安全边界。记录encoded-cache-hit及源缓存容量。

这仍不是全局A2准入，16MiB GPU部分依然是既有估算；活动表面、预载16MiB、字体、视频和驱动另有生命周期。源占用可能挤掉原本可保留的像素，重新解码也尚未移离主线程；必须比较实机长帧，不能仅靠减少读盘次数认定全面改善。

本地420项测试通过、14项忽略。新增：像素淘汰后同一Vec地址复用且无第二次源读取、像素预载把源交付后复用、显式替换/前缀失效、活动源也计入预算、多资源总额度、1920×1080背景三资源九次轮换仅三次源读取。保留原有热动画三帧循环、旧场景不挤出热动画、错误解码/上传重试和透明证明替换回归。Vita核心交叉编译通过；新策略实机收益待测。

## 能否扩大、原生多大

当前预载结果16MiB、闲置纹理/像素/源共16MiB；不是程序总内存32MiB。实机已经启用Extended Memory，但曾有96MiB预载试验耗尽/碎片导致分配失败，不能直接恢复该值。

2026-09-11对IDA 13341先health后只读复核PCSG01297：0x81201708读取`.ini.SurfaceCacheSize`，缺失分支值0；0x8132BB74原样传入0x8101DC64，写manager+40，+44用量清零。进一步追到0x81333B18将该整数配置初始值设为0；0x813311D4读取`SURFACE_CACHE_SIZE`并可覆盖该值。该游戏最终运行值、0的完整语义以及其他覆盖尚未动态确认，不能说原生总缓存为0或固定64/128MiB。既有64MiB CDRAM、256KiB CDRAM、128MiB uncached是分配池，不等价于贴图缓存。只读反编译证据位于build/native-five-audit/20260911-cache-defaults.json等同日文件。

SHUF00002已安装system.ini中WINDOWS的`SURFACE_CACHE_SIZE=67108864`被分号注释；VITA段显式FONT_CACHE_SIZE=25165824是字体额度，不能拿来当图片额度。未写入游戏目录，也未修改这些设置。

扩大到24/32MiB可作为后续受控对照选项，但需要分别测预载/闲置额度，记录峰值、失败回退和OGV切换；这次先固定额度比较保留策略。

## 预载交付优先级补充

源缓存是原有预载交付之后的回退：先调用既有prefetch回调，保留它的取结果／等待协议；若返回已准备像素就直接上传，没有交付结果才复用压缩源。避免旧源抢在新像素之前，引入重复解码及占着预载结果的问题。新增回归先淘汰旧图像素、保留旧源，再交付不同透明度的新像素和新源，确认零额外读盘／解码且下次源命中仍为新内容。总计421项通过、14项忽略，Vita交叉编译通过。6f26d74为短暂安装的中间候选，未要求用户开展对照测试；修正交付顺序后重新打包。

最终测试候选core 13efef9、host fb0d0aa：build/direct-candidates/encoded-source-13efef9。VPK SHA256为5909006b51288a71f74fcca7cb78287bdd61f0b22835ac4fcfa37deec19651aa；eboot为4bd0ea46b39eee3feab142c5d332807a2d99f31c7d3030dc350668d72821dcf6。host clean构建、来源校验、VPK CRC与SFO核对通过。deploy-20260911-003558完成备份、上传读回验证和启动。

启动日志build/hardware-logs/20260911-003646-current/host.log，SHA256为7559d4d3ab7551d842e4f2cff5f2fbbf1e8c20a03e22a191aaf5b41ec512e102：333MHz，retained/local_base/overlay验证通过、全部启用；启动菜单账本faults=0。此时未进入游戏，不能当作缓存收益或运行稳定性验证。下一步保持333MHz，SHUF00002重复经过同组背景／人物，比较首次与再次载入、encoded-cache-hit、读盘／解码／上传时长以及OGV进出、快进稳定性。

## 16MiB源保留版实机结果与24MiB对照

用户测试后复制日志20260911-003934-current，SHA256 f747ee067d23615914e77786a10fad0e2b076e96fe4e2db4c1b3e0aaf8c0e85b，证据保存于evidence/encoded-cache-20260911。12次源命中、最终22份源共3490500字节。zbg27k首次预载读盘446249us，需求等待863362us，交付像素；之后两次命中803091字节源，无该文件再次整读，但主线程解码分别309235／312185us，上传53405／52113us。GPU/像素回收日志直接显示先降级保留像素、随后19,138,033字节压力超过16MiB又删掉8,294,400字节背景像素；decoded_hits最终为0。

157829204us处转场总891481us，含背景312185us解码、52113us上传，人物79720us解码、59118us上传，wipe_13另读79661us／解码56124us。之前另一轮相近资源组合为1452049us；非同输入严格A/B，不把差值全算作缓存收益。首次冷加载仍会等待，源命中也不能消除解码。最后两个完整5秒窗各300帧、最大17314／17426us、over20ms=0；此前仍有运动/操作长帧，不能称全程稳定60。

30组完整资源快照无账本错误，已采样newlib used峰值73904216字节；账本不覆盖全部第三方内部开销，也不能代表最大连续空闲块。没有把用户的“好了”当作无缺块/无崩溃的明确反馈。

下一候选core 6176844只将闲置GPU/CPU/源共同估算额度由16改24MiB，预载16MiB和源单层4MiB上限不变；shader、异步等待协议和GPU安全边界不变。新增1920×1080背景加较新表面的压力对照：16MiB淘汰像素而再次解码，24MiB降级后保留像素并命中decoded；原16MiB三背景循环测试显式固定原额度。422项通过、14项忽略，Vita交叉编译通过。首次冷加载不因增大闲置缓存自动变快，实际收益、OGV峰值和快进稳定性待实机验证；不升级为已接受基线。

24MiB候选build/direct-candidates/idle24-6176844，host 934d0ad、core 6176844；VPK SHA256 666c2884e885270b5b68c78bd7b8d7b9526d012af81d0a42d497a9da9b1fef03、eboot ea332da407f7e209b8be8fb9c9eeec25d5650956aebeca680a8e94804b21d88b。host clean构建和来源校验通过。deploy-20260911-004258备份／读回／启动通过；20260911-004415-current启动日志SHA256 06cf1ae222da00e2c671e401fadebbe8b979c3a3009e15c906f37689506c895f，333MHz，retained/local_base/overlay启用，启动账本faults=0。尚待用户重复场景、快进和OGV测试。

## 24MiB实机：有小图像素命中，大背景仍被淘汰

20260911-004651-current日志SHA256 446165f1f9963a12791b254999636053ff32c5350cac39c15813464ffe546c13，保存于evidence/idle24-20260911。实际budget=25165824；decoded命中2次（msgoff 21100us上传、bg27k 19057us上传），源命中14次。zbg27k三次源命中后仍解码317679／314201／316174us；没有大背景decoded命中。先GPU降级保留像素，随后闲置压力26624549／27948581字节再次淘汰8294400字节背景像素，说明24MiB未覆盖这组切换的工作集。

154480984us处相近转场881301us，对比上一轮891481us差异很小，且手工输入不是严格A/B，不能宣称明显改善。最后两个5秒完整窗各300帧，max17485／17904us、over20ms=0；先前运动与切换仍有长帧。30组完整账本无错误；采样newlib used峰值70130808字节，CDRAM账本峰值80478208字节；预载残留量不同，不能拿两轮heap差值证明扩大缓存降低了内存。

下一步core 7db4f17仅将闲置额度提高到32MiB，预载16MiB、压缩源上限4MiB保持不变。压力回归扩展为两组较新表面、16／24／32MiB六种组合，确认跨过预算门槛后避免重复解码；422项通过、14项忽略，Vita交叉编译通过。此轮只验证容量，尚不改变LRU顺序、加载线程、shader和GPU生命周期。32MiB若仍频繁淘汰昂贵像素，应分析保留优先级与瞬时工作集，而非无限增大缓存。
