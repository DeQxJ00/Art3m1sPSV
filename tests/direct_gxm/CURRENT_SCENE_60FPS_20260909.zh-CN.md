# 60 FPS 页与 43 FPS 页的对照

2026-09-09 17:22，VitaCompanion 健康检查后仅回收日志，未改包、输入、shader 或等待。日志 `build/hardware-logs/20260909-172224-current/host.log`，SHA256 `40d7821a988939f810e0f6176a51a792c6aa2b3b646585f22270c5876c7ae640`。

最近一分钟仍混有场景变化，故 57.19 FPS 的整分钟平均值不代表当前停稳页。最后连续三个 5 秒窗口均 300 帧，平均帧时间约 16.65 ms，确认当前约 60 FPS。仍存在个别约 20–22 ms 的单帧。

| 指标 | 先前 43 FPS 页 | 当前 60 FPS 页 |
| --- | --- | --- |
| quads / draws | 137 / 26 | 69 / 23 |
| plain quads / effect uniform 次数 | 135 / 2 | 69 / 0 |
| CPU 绘制构建 | 约 8.0 ms | 约 7.1 ms |
| 其中快照同步 | 约 3.6–3.7 ms | 约 3.6 ms |
| begin 安全等待 | 约 9.8 ms | 约 1.18–1.19 ms |
| 逻辑更新 | 约 4.1 ms | 约 4.0 ms |
| present 总耗时 | 约 18.9 ms | 约 12.6 ms |
| queue 平均等待 | 约 0.04 ms | 约 3.5–3.7 ms |

等待时间在不同同步点之间发生变化；现在 queue 有空余节拍等待，不能把 begin 少掉的 8.6 ms 全部当作总帧节省。CPU 构建和快照开销几乎仍在，主要差异在当前绘制负载及 GPU 完成等待。旧页存在非 plain 路径，当前全部 plain，但不能由此认定某一 shader 错误或单独造成全部差距。

裁剪后重叠矩形面积和由 2,092,820 降至 1,736,284（约少 17%），仅是包围矩形统计，不是真实片元数/GPU 时间测量。纹理解码、上传为零，active_voice_hint=0。需要下一步对照具体绘制命令、覆盖范围和效果路径，才能区分普通透明叠绘与非 plain 路径的实际成本。

本次对照修正优化优先级：反复构建是常驻 CPU 浪费，但它不能单独解释这两个页面的 43/60 差异。优先定位低帧页面额外 GPU 负载及同步等待，保留现有已验证 shader 和资源生命周期安全边界。
