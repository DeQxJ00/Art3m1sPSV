# optL 实机翻页采样：2026-09-09 14:58

用户按要求翻页、停留并等待语音后回复完成。只通过 VitaCompanion 读取，未发送游戏输入、替换安装包或改变频率。实际仍为 optL，ARM 333 MHz，受保护的帧首等待开启。

日志备份：`build/hardware-logs/20260909-145803-companion/host.log`，867735 字节，SHA256 `CB01E67BEE0D30503D9B0A3C63E42810450AA8D735E9B455105343866645DD60`。
与上一轮 `build/profile-ab/20260909-145255/on-b.log` 完整行前缀逐字节匹配，确认同一次运行；只分析之后新增的 294795 字节，另存 `since-ab.log` 与 `since-ab-analysis.json`。新增部分含 25 次 Circle 按下记录，不能等同于 25 句（可能补全文字或切换场景）。

## 已确认

| 事件 | 实测 |
| --- | --- |
| `:bg/zbg27k`，1920×1080，两次解码 | 402.890 / 415.044 ms |
| 同一资源两次上传路径总耗时 | 145.232 / 138.699 ms，其中 host 调用 42.246 / 39.295 ms |
| `:fg/tor/z2/tor_z2a0100.png`，984×993 解码 | 123.578 ms |
| `:bg/bg27k`，960×540，两次解码 | 111.307 / 106.522 ms |
| 两个大资源切换窗口的最长宿主帧 | 817.056 / 751.361 ms |
| 其余若干活动窗口最长帧 | 69.883–258.863 ms；不能全部归为纯文字翻页 |

五秒窗口内的大资源解码与大峰值一致，且现有 `GxmTextureProvider::resolve` 确实在调用线程同步执行 source/read、图像解码、RGBA 转换和上传。因此它是明确的阻塞工作来源。但没有逐帧事件 ID，不能把不同统计分项直接相加为同一帧，也不能把最长帧全部归因于一张图。

重复的同名、同尺寸解码表明再次访问时没有命中已有纹理。当前空闲纹理预算为 16 MiB，计入 CPU RGBA 与 GPU 像素估计。一张 1920×1080 RGBA 的两份存储约 15.82 MiB，几乎占满预算；日志同时有淘汰计数增长。**预算竞争是强候选原因，但日志未按名称记录每次淘汰，尚不能排除显式失效/移除。** 不能凭此直接扩大预算而不测内存余量。

末尾两个稳定窗口没有捕获/上传活动，平均宿主分项合计约 20.55/20.58 ms（约 48.6 FPS）；与上一页 38–39 FPS 的场景和文字负载不同，不能算优化 A/B 收益。

语音生命周期有正常 close，活动语音提示返回 0、只剩一条音轨。新增部分音频统计记录 6 次工作块超过预算，输出错误为 0；不能称音频完全无影响，也没有证据需要通过降低采样率/精度解决这些图片加载峰值。

## 与之前结论的关系

静止长句约 12 ms/render 的绘制构建与本轮 100–400 ms 图片解码是不同问题，应分别优化。字体图集不足不能解释这一轮全部大卡顿；同时，本轮统计还不足以单独定位“仅换文字、不切背景”的每次峰值。

转场窗口出现全屏尺寸上传与 capture 时间，需结合 core capture 路径继续减少复制。大小相同不足以证明每次 960×540 上传都是截图，不做这个归因。

## 后续优先项

1. 对纹理逐名记录 budget eviction / explicit removal / source replacement，核对重复解码原因，避免盲目调大缓存。
2. 把可提前知道的资源解码移出正式绘制关键路径；发布已完成数据，GPU 创建/上传仍遵守渲染线程与同步边界。首用未就绪时必须保留正确转场语义，不能显示残缺新场景。
3. 继续既定的局部绘制数据复用，独立测纯文字换句与静态页面；CPU 候选尚未装入这轮实机，不混算收益。
4. 实机视觉是否仍有闪烁、缺块，需要用户观察补充；本次“好了”只表明操作完成，不能自动视为画面回归通过。

这轮仅取得证据，没有制作新安装包。
