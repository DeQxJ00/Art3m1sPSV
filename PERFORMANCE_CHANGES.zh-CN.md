# 实机录像反馈：第一轮修改与验证

日期：2026-09-07。优先测试 T01 SHUF00002。未改游戏脚本或资源，未在游戏运行目录解包。用户录像版本早于存档缩略图修复，不能将其中低帧归因于后来的截图等待。

## 已实现

- 加载阶段进度条：后台打开 PFS，以资源包数显示进度；core 初始化仍在主线程，阶段进度不是精确剩余时间。
- 资源预取：纹理 source 消费 bindSurface 保存的字节，保持 bind/unbind 引用计数；降低重复 IO，不代表图片解码缓存或真正异步预取已完成。
- Theora 箭头：静默、非循环图层使用后台彩色与遮罩解码；彩色解码器支持时使用两个 FFmpeg frame threads，遮罩仍单线程。主线程只按 PTS 消费已准备帧并上传，完成通知及 GPU 操作留在主线程。
- 三个 CPU 队列槽加一个消费缓冲；960×540 RGBA 共约 7.91 MiB，队列预算上限 16 MiB。此数不含解码器参考帧、转换工作区、core 副本与 GPU 纹理。分配或线程创建失败回退原路径，不预解码整段到无限缓存。
- 迟到处理：选择最新可呈现帧；已知时长时跳过过时彩色帧的转换、遮罩合成与上传，仍解码依赖帧且保留已知末帧。遮罩追赶只转换最终选择帧。
- 相同尺寸视频复用 NVG image 和 core RGBA 向量容量；只在视频更新时等待旧 GPU 读者。尚未实现 GPU 多缓冲纹理轮换，视频上传等待依然可能是瓶颈。
- 音频不降采样率或精度。新增重采样耗时与线程身份、亲和性、运行时钟计数，分别记录主线程、音频及 Theora 协调线程。`run_clocks` 是 SDK 时钟计数，不是 CPU 百分比；FFmpeg 内部两个解码线程未分别采样。音频 `decode_us` 包含重采样，不可与 `resample_us` 相加统计。

## 验证证据

| 检查 | 结果 / 日志 |
|---|---|
| core 桌面库测试 | 323 通过，4 忽略；`build/perf-core-tests.log` |
| 音频回归 | 44.1/48 kHz、单双声道、循环、淡入淡出、重采样、缓冲所有权与完成代次通过；`build/perf-audio-tests.log` |
| CPU 队列回归 | 时间戳选择、容量上限、并发生产、取消唤醒及 join 通过；`build/video-queue-tests.log` |
| 真实 FFmpeg Theora＋遮罩 | 64×64 与 960×540 合成 YUV444P、30 FPS、0.5 秒；主线程上传/通知、alpha、末帧停留、EOF、各 20 次取消与文件资源释放通过；`build/perf-video-tests.log` |
| 960×540 正常时序 | 桌面呈现 15 帧，播放时钟 500 ms |
| 960×540 人为卡顿 300 ms | 桌面呈现 9 帧，队列丢弃 2 帧，跳过转换 4 帧，播放时钟 501 ms |
| Vita 构建 | Rust core 与 GXM VPK 成功；`build/perf-core-build.log`、`build/perf-host-build.log` |

上述 C 回归启用 ASan/UBSan，未报错。合成素材只在工作区生成；桌面解码不替代 Vita FFmpeg/GXM 集成或实机性能验证。构建有 WSL 时钟偏差警告。最终包 `build/gxm-host/art3m1s_gxm.vpk`，15574537 bytes，SHA256 `71A9831C9DC47BAC0E862926EDCDBE4996B3F50E6A7DED76AB46289BFA60ABA3`。

## 尚待处理和实机验收

MCP `127.0.0.1:32560` 拒绝连接，最终包未安装运行。下一轮先用 T01 冷启动、普通场景切换、箭头进入/退出、存档缩略图及返回菜单，采集新 host.log 中 `[video-async]`、`[video-perf]`、`[audio-detail]`、`[thread-perf]`。同一场景、时钟设置和包版本对比，再决定 CPU/GPU 后续优化；未宣称实机达到 30 FPS。

章节跳过尚未改逻辑：原脚本依赖独立 gscr 全局已读状态，不依赖手动存档槽。需用独立空白保存根目录测试，不能删除用户现有全局数据或按游戏名禁用跳过。

原生 IDA 证据支持三重显示缓冲、同规格目标复用和 JPEG 工作区容量复用，尚不能证明通用多级场景 LRU。场景侧剩余工作包括真正异步预取/解码、有字节预算的 GPU 缓存，以及替换 CPU 转场读回的 GXM 离屏快照。详见 `PHYSICAL_RECORDING_REVIEW.zh-CN.md` 的调查依据。
