# 2026-09-08 实机日志分析

输入：用户提供 `G:/data/art3m1s-gxm/host.log`，文件修改时间 01:55:18；启动标识 build Sep 8 00:56:50。工作区保留副本 `build/physical-log-20260908-015518/host.log`。本次为日志及代码分析，未修改运行代码，未发布新包。

## 已证实

- 文件日志修复有效，主线程、音频和视频数据均已写入。
- `movie/logo.mp4` 使用 `h264_vita`、NV12-direct，首帧成功，三个 NV12 映射槽成功。解码约 2.1 ms/帧；稳定窗口主循环约 60 FPS。视频统计中的 `upload_us_per_frame` 在直接路径包含呈现/等待，不能当作 RGBA 上传开销。
- 01:55:08 附近 197 帧约用 5.1 秒，约 39 FPS；core present 平均 16.879 ms，逻辑平均 4.913 ms。01:54:32 附近约 32 FPS。不是只有瞬间 FPS 显示异常。
- 01:55:03 的 core present 最大 851.818 ms，对应整帧最大 855.407 ms。该计时覆盖 core 绘制路径，包括其内部加载、解码、纹理上传和命令准备；不包含最终 GXM 提交/呈现等待，不能直接归因于 GPU 或文字。
- 01:54:38 的整帧最大 5.140 秒，逻辑阶段均值异常升高而绘制最大仅 12.651 ms，另有逻辑阶段阻塞。启动时的 11.2 秒初始化和 13.6 秒逻辑长帧须与稳定游戏帧分开。
- 音频活动窗口每 2048 样本块平均工作约 0.865–4.980 ms，48 kHz 下块时长约 42.67 ms；多数重采样计时为零，输出错误和削波为零。偶有打开/命令处理造成超预算，不能说音频完全无问题，但当前不支持靠降低音频精度解决主要卡顿。
- 主线程运行计数远高于音频线程，是单核高负载的优先调查对象。线程可迁移核心，run_clocks 未换算为 CPU 百分比，不能据此声称锁定某一核心的 90%。

## 纹理路径的具体线索

缓存 miss 从 9 增到 866，淘汰仅从 1 增到 19，期间出现 `pc/ui/ja/mw/dummy` 缺失。代码确认 `resolve` 对失败资源只抑制重复告警，后续仍重新调用资源源；源又尝试 `.png` 和原路径。重复失败查找是需要测量的候选。

miss 包括失败查找，不能把 866 解释为 866 次 PNG 解码，日志也没有逐资源耗时，因此尚不能证明 dummy 是主要瓶颈。修复不能硬编码该游戏路径；若采用失败缓存，需要保留动态路径变化或资源稍后出现时的重试语义。

## 后续优先级及本次边界

1. 细分 core present 的资源读取、解码、上传、文字图集更新及绘制命令耗时，并区分 miss 成功/失败，定位约 852 ms 峰值。
2. 细分逻辑阶段的同步加载、字体加载及脚本事件耗时，定位约 5.14 秒长帧。
3. 根据测量决定纹理失败重试、文字图集更新或绘制缓存的优化，保留相同场景前后实机对比。
4. 本日志没有 Theora / video-async 箭头播放记录，无法评价 OGV 箭头优化；需要覆盖该效果的另一次日志。H.264 硬解成功不等于 Theora 硬解。

此次没有清理或修改用户游戏、存档及设备文件。
