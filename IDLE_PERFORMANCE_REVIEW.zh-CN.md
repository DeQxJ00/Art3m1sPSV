# 普通对话低帧录像

2026-09-08，`00-33-05.mp4`，37.317 秒。完整逐秒抽帧在 `build/idle-performance-review`。普通天空背景对话约 21–60 FPS 波动，室内约 14–57 FPS，约 29 秒切换景别出现 10 FPS；15–19 秒附近黑屏显示 60 FPS。单核负载多次超过 90%。不能据此确认高负载线程或称整个片段无工作负载：其中包含换句、字形生成、语音启动、文字动画及转场。

当前已确认的潜在热点：文字新增字形时上传整张 1024×1024 RGBA atlas（4 MiB），GXM 普通上传会重建图像；文字描边产生多条逐字绘制命令，宿主每条都走 NanoVG 矩形路径；场景重建会重新构建文字排版。其存在不等于已测得占据实机总耗时的比例。音频是独立 worker 且通过音频输出阻塞节流，没有证据支持直接降低其质量。

本轮添加每 5 秒聚合的 `[frame-perf]`：媒体泵、core 逻辑、UI/呈现、帧结束读回的平均墙钟耗时，最大帧耗时和超过 20 ms 次数；`[draw-perf]` 单独记录 core 绘制入口平均/最大耗时（不含最后的 GPU 提交/显示等待）；`[main-thread]` 记录主线程 ID、CPU、亲和性及累计 run_clocks。由 Borealis Logger 写入实机 `ux0:data/art3m1s-gxm/host.log`。run_clocks 不是 CPU 百分比，墙钟耗时包含等待；两个平均值相减不能视为纯 GPU 时间，因为还包含 UI 和宿主工作。

这次是诊断版本，不声称修复普通对话低帧，也没有降低音质、分辨率或跳过必要动画。需要用同一段实机流程的新 host.log 判定主要时间落在逻辑、core 绘制还是呈现等待，再针对图集局部更新、文字批量绘制或独立排版缓存优化。构建日志 `build/idle-profile-final-build.log`。模拟器 MCP 此前离线，实机性能仍需用户设备采样。
